var assert      = require('assert');
var should      = require('should');

describe('Profile', function(){

    it.skip('should log them in with github credentials', function(done){

    });

    it.skip('should fetch their username and access key', function(done){

    });

    it.skip('should be able to generate a new access key', function(done){

    });

});